# Pharm Track POS

Private pharmacy point-of-sale system built with Electron + React + SQLite.

## Setup
npm install
cd frontend && npm install && npm run build
cd .. && npm run dev

## Build for production
npm run dist
